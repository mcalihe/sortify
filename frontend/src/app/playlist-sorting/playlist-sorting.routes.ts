import { Routes } from '@angular/router';
import { PlaylistSortingComponent } from './components/playlist-sorting/playlist-sorting.component';

export const routes: Routes = [
    { path: '', component: PlaylistSortingComponent },
];
